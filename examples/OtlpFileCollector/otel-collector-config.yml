# OpenTelemetry Collector Configuration for JSONL Log Verification
# This configuration reads JSONL logs from a file and exports them to the Aspire Dashboard

receivers:
  # OTLP JSON File receiver - reads JSONL formatted logs
  otlpjsonfile:
    include:
      - /data/input/*.jsonl
    # Start from the beginning of the file
    start_at: beginning

  # Standard OTLP receiver (gRPC and HTTP)
  otlp:
    protocols:
      grpc:
        endpoint: "[::]:4317"
      http:
        endpoint: "[::]:4318"

processors:
  # Batch processor for efficiency
  batch:
    timeout: 10s
    send_batch_size: 100

exporters:
  # Debug exporter for troubleshooting
  debug:
    sampling_initial: 5
    sampling_thereafter: 1

  # OTLP exporter to Aspire Dashboard
  otlp_grpc:
    endpoint: aspire-dashboard:18889
    tls:
      insecure: true

  # OTLP file exporter - writes telemetry to files
  # Add this to the pipeline, then send example logs to OTLP receiver,
  # To see how the Collector generates files.
  file_traces:
    path: /data/output/traces.jsonl
    create_directory: true
    flush_interval: 5s
  file_metrics:
    path: /data/output/metrics.jsonl
    create_directory: true
    flush_interval: 5s
  file_logs:
    path: /data/output/logs.jsonl
    create_directory: true
    flush_interval: 5s

service:
  pipelines:
    traces:
      receivers: [otlpjsonfile, otlp]
      processors: [batch]
      exporters: [debug, otlp_grpc]
    metrics:
      receivers: [otlpjsonfile, otlp]
      processors: [batch]
      exporters: [debug, otlp_grpc]
    logs:
      receivers: [otlpjsonfile, otlp]
      processors: [batch]
      exporters: [debug, otlp_grpc]
